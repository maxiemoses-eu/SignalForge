# Increase resilience for slow network/proxy environments
fetch-retry-maxtimeout=120000
fetch-retries=5
# Security: Prevents the progress bar from leaking logs in CI
progress=false
# Ensure reproducible builds
save-exact=true